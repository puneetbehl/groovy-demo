def (x, y)  = [ 'Mackbook Pro', 'unknown' ]

if (x!=null && x.size() > 0) x else y

if (x && x.size()) x else y

if (x) x else y // Null empty zero sized false else true

x ? x : y // Good old ternary operator
x?:y // Elvis